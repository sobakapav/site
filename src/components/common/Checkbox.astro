---

const {
  id,
  label,
  type = 'checkbox',
  group,
  className = '',
  classLabel = 'px-4 py-[9px]',
} = Astro.props;

const aClassName = className + ' hidden';

---

<astro-checkbox class="inline-block">
  <input
    id={id}
    name={type == 'radio' ? (group || id) : id}
    type={type}
    class={aClassName}
    />
  <label for={id} class:list={["bg-[#E6E6E6] inline-flex items-center hover:cursor-pointer", classLabel]}>
    <span class="color-[#C4C4C4] hover:text-[--aw-color-hund]">
      {label}
    </span>
  </label>
</astro-checkbox>
