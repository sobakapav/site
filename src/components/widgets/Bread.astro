---
import { getHomePermalink } from '~/utils/permalinks';

const {
  title,
  parent,
  parentTitle
} = Astro.props;
---

<div class="flex flex-row text-base gap-2.5 py-10 px-6 mx-auto max-w-7xl text-sm text-[#9F9F9F]">
  <span>
    <a href={getHomePermalink()}>Главная</a>
  </span>
  { parentTitle && (
    <Fragment>
      <span>/</span>
      <span>
        {parent ? (
          <a href={parent}>{parentTitle}</a>
        ) : parentTitle }
      </span>
    </Fragment>
  )}
  { title && (
    <span>/</span>
    <span>
      { title }
    </span>
  )}
<div>
